      <!-- uses apache root for requests rewrited from apache -->
      <path-mapping url-regexp='^/apache2resin/(.*)' 
			real-path='/home/httpd/html/$1'/>
      <!-- uses resin root for requests rewrited from apache -->
      <!--
      <path-mapping url-regexp='^/resin(.*)' 
			real-path='/home/httpd/resin/$1'/>
      -->
